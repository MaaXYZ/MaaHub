{
  "scripts": {
    "dev": "yarn --cwd website dev",
    "build": "yarn --cwd website build",
    "preview": "yarn --cwd website preview",
    "astro": "yarn --cwd website astro",
    "validate:meta": "node scripts/validate-maahub-meta.mjs"
  }
}
